import React from "react";

const List = ({ listID, name }) => {
    return (
        <div>
            <h2>{name} (ID: {listID})</h2>
            <div>
                blah blah
            </div>
        </div>
    );
};

export default List;

import React from "react";

const List = ({ name }) => {
    return (
        <div>
            <h2>{name}</h2>
            <div>
                blah blah
            </div>
        </div>
    );
};

export default List;
